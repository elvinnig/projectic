import React from 'react';

// To render Cards in UserDashboard
const ProjectCards = ({title}) => {
    return (
        <li><span className=''>{title}</span></li>
    )
};
export default ProjectCards;